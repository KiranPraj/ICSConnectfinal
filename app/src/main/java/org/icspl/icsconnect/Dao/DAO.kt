/*
package org.icspl.icsconnect.Dao

import androidx.lifecycle.LiveData
import androidx.room.*
import org.icspl.icsconnect.models.user


@Dao
public interface DaoAccess {

    @Insert
    fun insert(vararg insert:user)

    @Update
    fun update(vararg insert:user)

    @Query("SELECT * FROM usertable WHERE id =:taskId")
    fun getTask(taskId: String): LiveData<user>

}*/
